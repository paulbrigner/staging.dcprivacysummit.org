 (cd "$(git rev-parse --show-toplevel)" && git apply --3way <<'EOF' 
diff --git a/css/styles.css b/css/styles.css
index ac491b2811c890dc709399ce840f8d723ce74415..91a78a66987b97cc9efdcc9c88018ab82ca0d39f 100644
--- a/css/styles.css
+++ b/css/styles.css
@@ -882,50 +882,62 @@ a:focus-visible {
 .program-card {
   background:
     linear-gradient(155deg, rgba(255, 255, 255, 0.95), rgba(239, 244, 255, 0.92));
   border-radius: var(--border-radius-lg);
   border: 1px solid rgba(16, 34, 72, 0.08);
   box-shadow: var(--shadow-card);
   padding: clamp(24px, 4.5vw, 34px);
   display: flex;
   flex-direction: column;
   gap: 18px;
 }
 @media (max-width: 720px) {
   .program-card {
     padding: clamp(16px, 5vw, 22px);
     gap: 14px;
   }
   .agenda-schedule .program-card {
     padding: 0;
     background: transparent;
     border: none;
     box-shadow: none;
     gap: 12px;
   }
 }
 
+@media (max-width: 640px) {
+  .session-speakers {
+    flex-direction: column;
+    align-items: stretch;
+    gap: 12px;
+  }
+
+  .session-speaker {
+    width: 100%;
+  }
+}
+
 /* Keynote cards */
 .keynote-grid {
   display: grid;
   gap: clamp(24px, 4vw, 36px);
   margin-top: clamp(40px, 6vw, 56px);
   grid-template-columns: repeat(auto-fit, minmax(360px, 1fr));
 }
 .keynote-card {
   flex-direction: row;
   align-items: flex-start;
   gap: clamp(22px, 3.5vw, 38px);
 }
 .keynote-media {
   flex-shrink: 0;
 }
 .keynote-media img {
   width: clamp(140px, 18vw, 180px);
   height: clamp(180px, 24vw, 228px);
   border-radius: var(--border-radius-lg);
   object-fit: cover;
   object-position: center;
   box-shadow: 0 18px 36px rgba(15, 23, 42, 0.22);
 }
 .keynote-body {
   display: flex;
 
EOF
)
